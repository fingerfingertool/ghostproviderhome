function domainGo(e){e.preventDefault();var v=document.getElementById('domainInput').value.trim().toLowerCase().replace(/[^a-z0-9-]/g,'');var r=document.getElementById('domainResult');if(!v){r.textContent='Type a name first.';return false}var tlds=['.com','.net','.io'];var h=0;for(var i=0;i<v.length;i++)h+=v.charCodeAt(i);var tld=tlds[h%tlds.length];var avail=(h%3!==0);r.textContent=avail?('✓ '+v+tld+' is available — claim it in the Studio bundle.'):('✕ '+v+tld+' is taken — try '+v+'hq'+tld+' or '+v+'.dev');return false}
document.querySelectorAll('[data-count]').forEach(function(el){var target=+el.dataset.count;var cur=0;var step=Math.max(1,Math.round(target/60));var iv=setInterval(function(){cur+=step;if(cur>=target){cur=target;clearInterval(iv)}el.textContent=cur.toLocaleString()+(target===99?'%':'+')},20)});
